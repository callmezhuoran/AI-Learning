# 当前学习状态

更新日期：2026-09-10。依据：本学习任务中用户实际反馈。

## 恢复点

- 当前课程：**D2：手写单头 causal attention**。
- 状态：**已布置，等待用户提交**；不能推定已经开始或完成。
- 上一课程：D1 的概念部分验收通过；环境部分由用户自行处理并明确要求跳过。
- 下一步：阅读 [D2 任务](lessons/day-02-causal-attention.md)，等待或检查用户的 attention 函数、概率矩阵、对照误差与两个解释。
- D2 完成后才进入逐 token 计算与 KV cache；不因为过了一天自动推进。

## 已确认掌握

1. 输入 `[2,16,1024]` 可解释为 batch、sequence、hidden dimension。
2. `Linear(1024,4096)` 含 bias 的参数数为 `1024*4096+4096`。
3. 线性层是矩阵乘法加 bias；所有 token 共享参数。
4. seq 从 16 到 32 时，参数数不变，输入输出元素数和该线性层的乘加次数翻倍。
5. FP32 到 FP16 改变每个元素的存储字节数，参数数量不变。
6. 10 亿参数 = 1B，纯权重 FP32 为 4 GB，FP16 为 2 GB（十进制）。

## 纠错记录

- 曾将输入/输出张量称为“输入/输出参数”：已解释参数与激活的区别，后续通过代码复核术语。
- 曾说 FP16“参数量减半”：已纠正为存储减半，用户通过 1B 参数检查题确认理解。
- 初次 PyTorch 命令在 PowerShell 中出现 Markdown 格式变形，未形成有效验证；此问题随后由用户自报解决。不要反复要求环境检查。

## 证据边界

- 已看到：口头推导、Windows `nvidia-smi`、WSL 配置输出。
- 用户报告：环境“我解决了”，要求跳过。
- 未看到：可运行的 D1 实现、WSL 内 PyTorch/CUDA 成功输出、D2 实现和实验结果。
- 因此：接受环境可继续的用户指示，但不宣称助教验证过 CUDA 或代码测试。
- 未记录任何个人 PR、测得的加速比或训练完成结果。

## D2 验收清单

- [ ] 实现 Q/K/V 后的 attention 四步：缩放点积、causal mask、softmax、与 V 相乘。
- [ ] 输出形状 `[2,4,8]`，概率每行和约 1，严格上三角为 0。
- [ ] 首个 token 输出等于首个 token 的 V（本实验无输出投影）。
- [ ] 改变最后输入 token 后，前三个输出保持不变。
- [ ] 与 PyTorch SDPA 对照，提交最大绝对误差；固定随机种子且关闭 dropout。
- [ ] 解释 softmax 最后一维表示对哪些位置归一化。
- [ ] 手算两个分数均为 0、第二个位置不可见时，解释 softmax 前后遮罩的区别。

## 日常维护

每天预算 120 分钟。用户最新明确反馈优先于旧计划；修改路线时保留简短原因。每次有效学习反馈都先追加日志、再更新本状态文件。任务布置、用户自报和实际验证必须区分。

## 存储与自动任务

- 本地位置：当前仓库目录。
- 远端：https://github.com/callmezhuoran/AI-Learning ，用户选择公开保存。
- 助教维护分支：`codex/learning-log`，用户已明确授权使用专门分支并开始维护；每日任务必须显式读取和更新该分支。
- 同步状态：连接可以读取，但实际写入返回 403；首次上传待 Git 写入认证或连接权限修复。不能仅凭仓库元数据中的 push=true 宣称当前连接可写。
- 既有自动任务：`ai-infra`，每天 09:00 Asia/Shanghai，在原学习任务中继续。
- 新任务/新环境需要明确打开本仓库；文件是恢复依据，不假设聊天记忆跨任务自动共享。
