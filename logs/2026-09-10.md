# 2026-09-10 学习记录

记录来源：本次学习对话。以下是整理后的学习摘要，不是完整聊天导出。

## 建立目标与路线

用户希望 2026 年底前找到 AI Infra 工作，关注量化、调度/算子优化和蒸馏；计划参与 SGLang/vLLM 并在 Mac、5070 Ti 上做贴近用户的实验。

根据用户基础把路线调整为每天 2 小时：推理与内存 → GPU 基础 → 框架调度 → 量化 → 热点优化 → 小规模蒸馏 → 项目/求职。十月开始逐步投递。详细计划在 roadmap.md。

用户后续明确要求每天早上 9 点通知；已建立原学习任务内的每日自动任务 `ai-infra`。

## D1：线性层与内存

用户正确解释 `[2,16,1024]` 为 batch、seq、hidden dimension；提出参数数 `1024*4096+4096`；理解矩阵乘法加 bias。

助教补充：每个 token 的每个输出特征是输入 1024 个特征的加权和；所有 batch/token 共享参数；bias 按位置广播；输出 `[2,16,4096]`。线性层本身不混合不同 token 信息。

用户正确判断 seq 翻倍不改变参数数量，输入输出元素和乘加次数翻倍。用户最初表述“FP16 参数量减半”，助教纠正为存储字节减半，参数数量不变，并区分参数和激活。

最终检查题用户回答：10 亿参数等于 1B；FP32 纯权重 4 GB，FP16 为 2 GB；切换精度不影响参数数量。判定：**D1 概念验收通过**。

没有收到 D1 可运行实现或测量数据，不宣称代码验证已完成。

## 环境

用户贴出 Windows nvidia-smi：RTX 5070 Ti，16303 MiB 总显存，驱动 591.74，显示 CUDA 13.1。

用户贴出 WSL：Ubuntu-22.04 为默认分发且版本 2；Windows Python 3.13.5。一次 PyTorch 命令出现格式变形且没有有效结果。

随后用户明确说“先跳过，这个我解决了”。接受该指示并继续教学；具体 WSL/PyTorch 版本和 GPU 运算结果仍未由助教验证。后续不反复追问环境。

## D2：已布置，未收到结果

任务：CPU/FP32、B=2/T=4/D=8 的单头 causal attention，手写缩放点积、mask、softmax、V 聚合。

验收：形状、行和、上三角零、首 token 输出等于自身 V、修改最后 token 不影响前三输出、SDPA 对照误差。解释 softmax 轴与 mask 顺序。

当前没有用户代码、概率矩阵、误差或解释，状态为 **等待提交**。详细题目在 lessons/day-02-causal-attention.md。

## 建立持久记录

用户担心聊天无法长期记住学习进度，要求在云端或 GitHub 建立记录仓库，并创建了 https://github.com/callmezhuoran/AI-Learning.git 。用户随后将仓库调整为 public，继续保存学习内容。学习档案包含路线、课程、反馈日志、恢复点和实验模板；后续课程从文件恢复进度，以实际提交与反馈更新。

公开后连接可以读仓库，创建 README 的实际请求返回 `403 Resource not accessible by integration`，因此没有远端提交。先保存本地版本并让每日任务从本地文件恢复，首次上传待完成。

用户明确授权助教设置操作分支并开始维护，已选择 `codex/learning-log`。学习记录、恢复规则与每日任务均以此分支为入口；不自动合并到其他分支。随后尝试通过本地 Git 完成首次推送。

用户完成 GitHub 认证后，本地 Git 首次推送成功：远端已建立 `codex/learning-log`，包含 13 个文件；已读取远端 STATE.md 核对学习状态。每日 09:00 任务已改为从本地档案和该远端分支恢复进度，并在后续反馈后更新记录。首次推送包含提交 `3d5cc2f` 与 `610b22c`，随后提交同步状态更新。

## 恢复 Day 2 题目

用户要求“继续吧，看看 Day 2 的题目是什么”。已读取辅导规则、状态、学习者信息、D2 题目和日志，并核对远端没有更新。继续原有 D2：形状推导、四步实现、正确性与因果性验证、softmax 轴及 mask 顺序解释。预算维持 20+45+35+20=120 分钟；先请用户解释 QK 转置乘积的形状和矩阵元素含义。此次属于重新展示任务，不是完成证据，D2 仍等待提交。
