# AI Infra 学习辅导维护规则

这是用户的学习记录仓库。按以下流程恢复与维护，用户最新明确指示优先。

## 开始辅导前

1. 先读 `STATE.md`、`learner-profile.md`。
2. 读当前课程文件和最近的 `logs/` 记录；必要时读 `roadmap.md`。
3. 有远端访问时先检查最新版本，保留用户在本地/远端的改动；不要覆盖未知变更或强制推送。
4. 区分已布置、用户自报完成、助教看到证据并验收。不能把缺失日志当作已完成。

## 当前教学约定

- 中文，每天约 120 分钟，C++/Python 基础较好，CUDA/Triton 从零开始。
- D1 概念已通过。用户已自行处理环境并要求跳过，除非后来出现具体运行问题，不再重复检查环境。
- D2 已布置但尚无提交；不要直接安排 D3 或声称 D2 已完成。
- 先让用户解释/实现，再针对证据纠错。作业脚手架保留 TODO，不替用户填答案并声称其掌握。
- 项目实验与 PR 并行；只有用户自己的理解、实现和验证才能作为求职证据。

## 每次反馈后必须记录

1. 将反馈、证据、纠错、剩余问题追加到当天 `logs/YYYY-MM-DD.md`。同日多轮按条目追加，不覆盖历史。
2. 更新 `STATE.md` 的恢复点、完成状态、待验证项和下一步。
3. 如果改了路线或每日任务，更新对应课程/计划并简记原因。
4. 本地有 Git 时提交本次相关文件。远端已配置且有访问能力时同步，使用正常快进；冲突时保留两方内容并处理，禁止强推。
5. 只有收到远端成功响应并核对文件后才声称云端同步成功；失败时保存本地并明确告知未同步。

## 每日自动任务

- 用户要求每天 09:00 Asia/Shanghai 发送课程安排；沿用原自动任务，不创建重复提醒。
- 先读本仓库状态，再结合原学习任务最新反馈。
- 没有新反馈时给当前课的精简续做任务，不猜测已完成，不堆积欠账。
- 新课程包含目标、2～3 个概念/问题、实现或实验步骤、总计 120 分钟的分配、验收标准和反馈方式。
- 每次课程发出后记录“已布置”，不记录为“已完成”。

## 数据与证据

- 不编造 benchmark、加速比、PR 合并状态、训练结果或用户完成记录。
- 记录硬件、模型 revision、框架版本、精度、输入输出长度、负载、预热、重复次数、原始结果和局限。
- 教师生成的参考结果与用户实验结果分别标注。
- 不提交凭据、完整聊天导出、无关个人进程列表、模型权重、大型数据或缓存。
- 不因本仓库存在就假设其他聊天能自动访问；需要用户或任务显式提供仓库位置和访问权限。

## 仓库地址与恢复

远端为 `https://github.com/callmezhuoran/AI-Learning`，main 分支，用户选择公开保存。当前任务的本地副本在其工作区 `outputs/ai-infra-learning/`。有新反馈时同步两处；外部环境可直接从远端读取恢复点。写入失败时保留本地改动并记录待同步，不假装推送已完成。
